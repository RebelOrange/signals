#!/usr/bin/env python3
"""Setup file for rfnoc-ada_filt module.

Copyright 2024 Ettus Research, a National Instruments Company

SPDX-License-Identifier: GPL-3.0-or-later
"""

from setuptools import find_packages, setup

packages = find_packages()

print("Including packages in rfnoc-ada_filt:", packages)

setup(
    name="rfnoc_ada_filt",
    version="${VERSION_MAJOR}.${VERSION_API}.${VERSION_ABI}",
    description="rfnoc-ada_filt: An example module for RFNoC OOT Python support",
    classifiers=[
        "Development Status :: 4 - Beta",
        "Programming Language :: C++",
        "Programming Language :: Python",
        "Topic :: System :: Hardware :: Hardware Drivers",
    ],
    keywords="SDR UHD USRP",
    author="Ettus Research",
    author_email="uhd.maintainer@emerson.com",
    url="https://www.ettus.com/",
    license="GPL-3.0-or-later",
    package_dir={"": r"${NATIVE_CURRENT_BINARY_DIR}"},
    package_data={"rfnoc_ada_filt": ["*.so"]},
    zip_safe=False,
    packages=packages,
    install_requires=[],
)
